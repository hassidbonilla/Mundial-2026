# Mundial

Proyecto backend para simulación del mundial.

- Ejecutar: `pnpm start`
- Desarrollo: `pnpm dev`
- Tests: `pnpm test`

Estructura principal:

- `app.js` - punto de entrada
- `src/` - código fuente (controllers, routes, services, config)

Añade más detalles según necesites.
